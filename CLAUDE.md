# CLAUDE.md

이 파일은 Claude Code (claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

**RunningCourse**는 위치 기반 러닝 코스 커뮤니티 웹앱입니다.
주요 기능: 지도 기반 코스 탐색 (TMap SDK), 폴리라인으로 코스 생성, 사용자 인증 (Google OAuth + 익명 세션), 좋아요 기능.

## 주요 명령어

```bash
npm run dev              # 개발 서버 (localhost:3000)
npm run build            # 프로덕션 빌드
npm run lint             # ESLint 실행
npm run storybook        # Storybook (localhost:6006)
npm run build-storybook  # Storybook 정적 사이트 빌드
npx vitest               # Storybook 컴포넌트/인터랙션 테스트 (Vitest 브라우저 모드)
npm run migrate:route-paths  # path_data 마이그레이션 (일회성, .env.local 필요)
```

## 아키텍처

**스택:** Next.js 14 App Router · TypeScript · CSS Modules · Supabase (인증 + DB) · TMap Vector SDK

**데이터 흐름:**

```
Page/Component → Server Action (src/actions/) → Service (src/services/) → Repository (src/repositories/) → Supabase
```

- **Server Actions**: 인증, 코스 CRUD, 폼 제출 등 mutation 처리
- **Repositories**: Supabase를 직접 호출하는 유일한 레이어
- **Services**: Actions와 Repositories 사이의 비즈니스 로직
- **Server Components**: 데이터 패칭; Client Components (`'use client'`)는 인터랙션 상태 관리

**주요 디렉토리:**

- `src/app/` — Next.js App Router 페이지 & 레이아웃
- `src/actions/` — 인증, 코스, 유저 관련 Server Actions
- `src/components/` — 기능별 컴포넌트 (`home/`, `courses-detail/`, `courses-list/`, `mypage/`, `course-submit/`, `tmap/` 등)
- `src/commons/` — 공통 UI 컴포넌트, 훅, 상수, 프로바이더, 타입, 유틸
- `src/repositories/` — 데이터 접근 (course, user, map 레포지토리)
- `src/services/` — 비즈니스 로직 (course, user, map 서비스)
- `src/lib/supabase/` — 서버, 클라이언트, 미들웨어용 Supabase 클라이언트 설정

## 인증

`AuthProvider` (`src/commons/providers/auth/`)가 세 가지 플래그를 제공합니다:

- `isLoggedIn` — 세션 존재 여부 (게스트/익명 포함)
- `isAuthenticated` — 실제 OAuth 로그인 유저 (익명 아님)
- `isAnonymous` — 게스트 세션

미들웨어 (`middleware.ts`)는 모든 요청에서 세션을 갱신하고 비공개 라우트를 보호합니다. 세션이 없는 경우(완전 미로그인)만 차단하고 `/login`으로 리다이렉트합니다. 익명(게스트) 세션은 라우트를 통과하나, 코스 생성·수정·좋아요 등 mutation은 Action 레벨에서 `isAnonymous` 여부로 별도 차단됩니다.

**비공개 라우트:** `/courses/new`, `/courses/[id]/edit`, `/mypage`

## DB 스키마 (Supabase Postgres)

- `users`: `id, nickname, profile_image_url, is_anonymous`
- `routes`: `id, user_id, title, description, distance_meters, path_data (JSON), start_lat, start_lng, start_address_region, image_urls, likes_count, created_at, updated_at`
- `route_likes`: 복합키 `(user_id, route_id)`
- 스토리지 버킷: `course_images` (공개)

## 환경 변수

`.env.local`에 필수 설정:

```
NEXT_PUBLIC_TMAP_API_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

## 코딩 컨벤션

- **스타일:** 컴포넌트별 CSS Modules; 인라인 스타일 금지. 색상·타이포그래피 등 상수는 `src/commons/constants/`에 위치.
- **경로 별칭:** `@/*` → `src/*`
- **컴포넌트:** 스토리 (`.stories.tsx`), 스타일 (`.module.css`), 컴포넌트 전용 훅을 같은 디렉토리에 위치.
- **`console.log` 사용 금지** (ESLint 경고); `console.error/warn/info`는 허용.
- **React Strict Mode** 비활성화 (`next.config.mjs`).
- import 순서는 ESLint로 강제: 외부 → 내부 → 상대경로.
- **SVG:** `next.config.mjs` webpack 설정으로 `.svg`를 `import`로 사용 가능 (asset/resource로 처리됨).

## Git 워크플로우

- `main` — 프로덕션 전용, 직접 커밋 금지
- `dev` — 통합 브랜치; 모든 기능은 PR + 1명 이상 승인 후 머지
- 기능 브랜치: `feat/<기능명>`

1.  코딩 전 생각하기
    가정하지 마세요. 혼란을 숨기지 마세요. 트레이드오프를 드러내세요.

구현 전에.

- 가정을 명시적으로 밝히세요. 불확실하면 질문하세요.
- 해석이 여러 가지라면 제시하세요. 조용히 하나만 고르지 마세요.
- 더 단순한 방법이 있다면 말하세요. 타당할 때는 반론을 제기하세요.
- 불명확한 것이 있으면 멈추세요. 무엇이 헷갈리는지 말하고, 질문하세요.

2. 단순함 우선
   문제를 해결하는 최소한의 코드만 작성하세요. 추측성 코드는 금지합니다.

- 요청받지 않은 기능은 추가하지 마세요.
- 단일 사용 코드에 추상화를 도입하지 마세요.
- 요청되지 않은 "유연성"이나 "설정 가능성"은 넣지 마세요.
- 불가능한 시나리오에 대한 에러 처리는 하지 마세요.
- 200줄을 작성했는데 50줄로 될 수 있다면, 다시 작성하세요.
- "시니어 엔지니어가 이걸 보고 과도하게 복잡하다고 할까?" 라고 자문하세요. 그렇다면 단순화하세요.

3. 외과적 변경
   꼭 필요한 것만 건드리세요. 자신이 만든 문제만 정리하세요.

기존 코드를 수정할 때.

- 인접한 코드, 주석, 포매팅을 "개선"하지 마세요.
- 망가지지 않은 것은 리팩토링하지 마세요.
- 자신이 다르게 했을지라도 기존 스타일에 맞추세요.
- 관련 없는 죽은 코드를 발견하면 언급하되, 삭제하지 마세요.

변경으로 인해 고아가 된 코드가 있을 때.

- 자신의 변경이 만든 사용되지 않는 import/변수/함수는 제거하세요.
- 이미 존재하던 죽은 코드는 요청받지 않는 한 제거하지 마세요.
- 테스트: 변경된 모든 줄은 사용자의 요청에 직접적으로 추적 가능해야 합니다.

4. 목표 중심 실행
   성공 기준을 정의하세요. 검증될 때까지 반복하세요.

작업을 검증 가능한 목표로 변환하세요.

- "유효성 검사 추가" → "잘못된 입력에 대한 테스트를 작성한 후 통과시키기"
- "버그 수정" → "버그를 재현하는 테스트를 작성한 후 통과시키기"
- "X 리팩토링" → "전후로 테스트가 통과하는지 확인"

여러 단계 작업에는 간단한 계획을 명시하세요.

1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]

명확한 성공 기준은 독립적으로 반복 작업을 가능하게 합니다. 약한 기준("작동하게 해줘")은 끊임없는 확인을 요구합니다.

5. 한국어 출력 시 마침 콜론 금지
   한국어 문장은 마침표로 끝내세요, 콜론이 아니라.

사용자가 한국어로 작성하면 출력도 한국어로.

- 다음 줄이 목록이나 예시여도 : 로 문장을 끝내지 마세요.
- 영어 문서로 훈련된 LLM은 콜론 습관을 한국어에도 적용합니다. 주의하세요.
- 테스트: 모든 한국어 문장 종결어는 ., ?, ! 이어야 합니다. : 은 안 됩니다.
- 콜론은 코드, 키-값 쌍, 레이블 내부에서는 허용됩니다. 문장 종결로는 안 됩니다.

6. 새 파일에 한국어 헤더 주석 달기
   모든 새 소스 파일의 첫 줄: 해당 파일의 역할을 설명하는 한 줄짜리 한국어 주석.

새 파일을 만들 때.

- TypeScript/JavaScript: // 사용자 인증 상태를 관리하는 Context Provider
- Python: # KIS API 호출을 비동기로 래핑하는 클라이언트
- SQL: -- 일별 집계 결과를 저장하는 머티리얼라이즈드 뷰
- 필수 지시어('use client', 'use server', shebang) 바로 아래에 작성하세요.
- 설정 파일(\*.config.ts, package.json 등)은 제외합니다.

이유: 에이전트는 코드베이스 전체가 아닌 파일을 선택적으로 읽습니다. 한 줄짜리 한국어 헤더는 다음 세션(사람이든 에이전트든)이 파일 전체를 다시 읽지 않고도 탐색할 수 있는 즉각적인 맥락을 제공합니다.

7. 계획 + 체크리스트 + 컨텍스트 노트
   비자명한 작업을 시작하기 전, 세 가지 산출물을 만드세요. 없이는 코딩을 시작하지 마세요.

- "버그 수정" → "버그를 재현하는 테스트를 작성한 후 통과시키기"
- "X 리팩토링" → "전후로 테스트가 통과하는지 확인"

여러 단계 작업에는 간단한 계획을 명시하세요.

1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
3. [단계] → 검증: [확인 방법]

명확한 성공 기준은 독립적으로 반복 작업을 가능하게 합니다. 약한 기준("작동하게 해줘")은 끊임없는 확인을 요구합니다.

5. 한국어 출력 시 마침 콜론 금지
   한국어 문장은 마침표로 끝내세요, 콜론이 아니라.

사용자가 한국어로 작성하면 출력도 한국어로.

- 다음 줄이 목록이나 예시여도 : 로 문장을 끝내지 마세요.
- 영어 문서로 훈련된 LLM은 콜론 습관을 한국어에도 적용합니다. 주의하세요.
- 테스트: 모든 한국어 문장 종결어는 ., ?, ! 이어야 합니다. : 은 안 됩니다.
- 콜론은 코드, 키-값 쌍, 레이블 내부에서는 허용됩니다. 문장 종결로는 안 됩니다.

6. 새 파일에 한국어 헤더 주석 달기
   모든 새 소스 파일의 첫 줄: 해당 파일의 역할을 설명하는 한 줄짜리 한국어 주석.

새 파일을 만들 때.

- TypeScript/JavaScript: // 사용자 인증 상태를 관리하는 Context Provider
- Python: # KIS API 호출을 비동기로 래핑하는 클라이언트
- SQL: -- 일별 집계 결과를 저장하는 머티리얼라이즈드 뷰
- 필수 지시어('use client', 'use server', shebang) 바로 아래에 작성하세요.
- 설정 파일(\*.config.ts, package.json 등)은 제외합니다.

이유: 에이전트는 코드베이스 전체가 아닌 파일을 선택적으로 읽습니다. 한 줄짜리 한국어 헤더는 다음 세션(사람이든 에이전트든)이 파일 전체를 다시 읽지 않고도 탐색할 수 있는 즉각적인 맥락을 제공합니다.

7. 계획 + 체크리스트 + 컨텍스트 노트
   비자명한 작업을 시작하기 전, 세 가지 산출물을 만드세요. 없이는 코딩을 시작하지 마세요.

- 계획 — 무엇을 왜 만드는가.
- 체크리스트(checklist.md) — 체크박스 형태의 구체적인 할 일. 완료되면 체크하세요.
- 컨텍스트 노트(context-notes.md) — 작업 중 내린 결정과 그 이유. 계속 추가하세요.

사용자가 계획만 주고 코딩을 시작하라고 하면, 멈추고 물어보세요. "체크리스트와 컨텍스트 노트를 먼저 만들까요?" 다음 세션 — 자신이든 다른 사람이든 — 은 모든 결정을 다시 도출하지 않고 이어받기 위해 노트가
필요합니다.

8. 완료 전 테스트 실행
   코드를 건드렸다면, "완료"라고 말하기 전에 테스트를 실행하세요.

- npm test, pytest, cargo test, 프로젝트에서 사용하는 것이라면 무엇이든 실행하세요.
- 테스트가 통과하면 결과를 보고하세요. 실패하면 수정하고 다시 실행하세요.
- 테스트 환경이 없다면 최소한 프로젝트가 빌드/컴파일되는지 확인하세요.
- 사용자가 "끝", "완료", "다 됐어" 신호를 보내기 전에 선제적으로 테스트를 실행하세요. 이후가 아니라.

이것이 LLM이 가장 자주 건너뛰는 단계입니다. 협상 불가로 취급하세요.

9. 시맨틱 커밋
   하나의 논리적 변경이 완료되면 커밋하세요. 사용자가 요청할 때까지 기다리지 마세요.

- 테스트: "이 커밋을 한 문장으로 설명할 수 있는가?" 가능하면 커밋하세요. 안 된다면 변경 사항이 아직 혼합된 것입니다. 분리하세요.
- 좋은 예: "auth 미들웨어 추가". 나쁜 예: "auth 추가하고 UI도 고치고 버그도 수정" (3개로 분리하세요).
- 롤백 능력을 잃을 때까지 관련 없는 20개의 수정을 누적하지 마세요.
- 의미 없이 커밋하지 마세요. 의미 있는 단위만.

참고: 솔로 프로토타입이나 일회성 스크립트는 속도를 위해 커밋을 느슨하게 묶어도 됩니다. 핵심은 되돌릴 수 있는 능력이지, 형식이 아닙니다.

10. 에러를 읽으세요, 추측하지 마세요
    실제 에러/로그 줄을 읽으세요. 기억에 의존한 패턴 매칭은 금물입니다.

무언가 실패했을 때.

- 전체 에러 메시지와 스택 트레이스를 읽으세요.
- 예상하는 내용이 아닌 실제 로그 출력을 확인하세요.
- 원인을 확인하기 전에 "흔한 수정법"을 적용하지 마세요.
- 불명확하면 print/log를 추가해 상태를 확인한 후 수정하세요.
