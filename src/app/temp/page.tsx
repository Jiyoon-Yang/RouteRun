import MapTest from "./MapTest";

/**
 * /temp 라우트 페이지
 * - 코스 등록 테스트용 카카오맵 컴포넌트를 보여줍니다.
 */
export default function TempPage() {
  return (
    <main className="min-h-screen bg-white text-black">
      <MapTest />
    </main>
  );
}
