가이드) `addtional_text` 컴포넌트 Storybook 스토리를 구현할 때 사용하는 템플릿.

목표)
- `src/commons/components/input/addtional_text/index.tsx` 구현을 검증할 수 있는 스토리를 작성한다.
- 상태(`default | success | error`), `showIcon` 토글, `message` 전달/미전달 케이스를 빠르게 확인 가능하게 구성한다.
- 구현 결과를 체크리스트로 반환한다.

==============================================

조건-커서룰)
- 아래 커서룰을 적용하고, 작업 종료 시 적용 결과를 체크리스트로 반환.
  - @01-common.mdc

==============================================

조건-파일경로)
- 참고 TSX: `src/commons/components/input/addtional_text/index.tsx`
- 참고 CSS: `src/commons/components/input/addtional_text/styles.module.css`
- 구현 STORIES: `src/commons/components/input/addtional_text/index.stories.tsx`

==============================================

핵심요구사항)
1) CSF 기본 구조
- `title`, `component`, `tags`, `args`, `argTypes`, `parameters.layout`를 포함할 것.
- 기본 확인용 `Playground` 스토리를 포함할 것.

2) 컨트롤 구성 원칙
- 핵심 props를 컨트롤에 연결:
  - `message` (text)
  - `state` (`default | success | error`)
  - `showIcon` (boolean)
- `className`은 `control: false`로 비활성화할 것.

3) 필수 스토리 세트
- `Playground`
- `States` (`default/success/error` 비교)
- `WithoutMessage` (빈 문자열 전달 시 렌더 생략 확인)

4) 검증 가능성
- 스토리만으로 상태별 컬러 차이와 message 유무 동작을 빠르게 검증할 수 있어야 함.
- 피그마에 없는 상태/옵션은 추가하지 않을 것.

체크리스트(출력 포맷)
- [ ] `src/commons/components/input/addtional_text/index.stories.tsx`에 스토리가 구현됨
- [ ] `Playground/States/WithoutMessage` 스토리가 포함됨
- [ ] `message`, `state`, `showIcon` 컨트롤이 동작함
- [ ] message 미전달(빈 문자열) 케이스에서 렌더가 생략됨
